/* File: css/styles.css */
:root {
    --bg: #071018;
    --surface: #0d1822;
    --surface-light: #f4f7fa;
    --text: #e8eef4;
    --text-dark: #18212a;
    --muted: #9aa9b6;
    --border: #263746;
    --accent: #54e1ff;
    --accent-strong: #16b9df;
    --accent-soft: rgba(84, 225, 255, 0.12);
    --danger: #ff7b8a;
    --shadow: 0 24px 60px rgba(0, 0, 0, 0.28);
    --radius: 18px;
    --max-width: 1180px;
    font-family:
        Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont,
        "Segoe UI", sans-serif;
}

* {
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

body {
    margin: 0;
    color: var(--text-dark);
    background: var(--surface-light);
    line-height: 1.6;
}

code,
pre {
    font-family:
        "Cascadia Code", "SFMono-Regular", Consolas, "Liberation Mono", monospace;
}

code {
    color: var(--accent-strong);
}

.container {
    width: min(calc(100% - 40px), var(--max-width));
    margin-inline: auto;
}

.site-header {
    position: sticky;
    top: 0;
    z-index: 1000;
    border-bottom: 1px solid var(--border);
    background: rgba(7, 16, 24, 0.92);
    backdrop-filter: blur(16px);
}

.header-content {
    min-height: 68px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
}

.brand {
    color: var(--text);
    font-weight: 800;
    letter-spacing: -0.02em;
    text-decoration: none;
}

.main-nav {
    display: flex;
    flex-wrap: wrap;
    gap: 18px;
}

.main-nav a {
    color: var(--muted);
    font-size: 0.92rem;
    text-decoration: none;
}

.main-nav a:hover,
.main-nav a:focus-visible {
    color: var(--accent);
}

.hero {
    min-height: 72vh;
    display: grid;
    align-items: center;
    color: var(--text);
    background:
        radial-gradient(circle at 80% 20%, rgba(84, 225, 255, 0.16), transparent 30%),
        linear-gradient(145deg, #071018, #101d29 60%, #071018);
}

.hero-content {
    padding-block: 100px;
}

.eyebrow {
    margin: 0 0 12px;
    color: var(--accent-strong);
    font-size: 0.75rem;
    font-weight: 800;
    letter-spacing: 0.16em;
}

.hero h1 {
    max-width: 850px;
    margin: 0;
    font-size: clamp(3rem, 7vw, 6.8rem);
    line-height: 0.98;
    letter-spacing: -0.055em;
}

.hero-text {
    max-width: 700px;
    margin: 28px 0;
    color: var(--muted);
    font-size: 1.15rem;
}

.button {
    display: inline-block;
    padding: 12px 18px;
    border: 1px solid var(--accent);
    border-radius: 999px;
    color: #041018;
    background: var(--accent);
    font-weight: 800;
    text-decoration: none;
    cursor: pointer;
}

.button:hover,
.button:focus-visible {
    background: #8aebff;
}

.button-secondary {
    width: 100%;
    color: var(--text);
    background: transparent;
}

.section {
    padding-block: 96px;
}

.section-dark {
    color: var(--text);
    background: var(--bg);
}

.section-light {
    background: #eef2f5;
}

.section-heading {
    max-width: 760px;
    margin-bottom: 42px;
}

.section-heading h2 {
    margin: 0 0 12px;
    font-size: clamp(2rem, 4vw, 3.6rem);
    line-height: 1.05;
    letter-spacing: -0.04em;
}

.section-heading p:last-child {
    margin: 0;
    color: #657482;
}

.section-dark .section-heading p:last-child {
    color: var(--muted);
}

.concept-grid,
.position-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 18px;
}

.concept-card,
.position-card {
    min-height: 230px;
    padding: 28px;
    border: 1px solid #d9e1e7;
    border-radius: var(--radius);
    background: white;
    box-shadow: 0 10px 30px rgba(25, 45, 60, 0.06);
}

.concept-card h3,
.position-card h3 {
    margin: 8px 0 10px;
}

.concept-card p,
.position-card p {
    color: #667582;
}

.concept-number {
    color: var(--accent-strong);
    font-weight: 900;
}

.playground {
    display: grid;
    grid-template-columns: 280px minmax(0, 1fr);
    gap: 24px;
}

.controls,
.computed-panel {
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background: var(--surface);
}

.controls {
    display: grid;
    align-content: start;
    gap: 18px;
    padding: 22px;
}

.control-group {
    display: grid;
    gap: 7px;
}

.control-group label {
    display: flex;
    justify-content: space-between;
    gap: 12px;
    color: var(--muted);
    font-size: 0.85rem;
    font-weight: 700;
}

.control-group select,
.control-group input {
    width: 100%;
}

.control-group select {
    padding: 10px 12px;
    border: 1px solid var(--border);
    border-radius: 10px;
    color: var(--text);
    background: #071018;
}

.control-group input {
    accent-color: var(--accent);
}

.playground-stage {
    min-width: 0;
}

.demo-container {
    position: relative;
    min-height: 440px;
    overflow: visible;
    padding: 42px;
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background:
        linear-gradient(rgba(84, 225, 255, 0.04) 1px, transparent 1px),
        linear-gradient(90deg, rgba(84, 225, 255, 0.04) 1px, transparent 1px),
        #09141d;
    background-size: 24px 24px;
}

.demo-item,
.demo-target {
    padding: 14px 18px;
    border-radius: 10px;
    color: #061019;
    font-weight: 800;
}

.demo-item {
    width: 180px;
    background: #758592;
}

.demo-item-after {
    margin-top: 18px;
}

.demo-target {
    border: 2px solid var(--accent);
    background: rgba(84, 225, 255, 0.18);
}

.demo-long-content {
    display: block;
    margin-top: 16px;
    color: var(--text);
    font-weight: 500;
}

.computed-panel {
    margin-top: 18px;
    padding: 20px;
}

.computed-panel h3 {
    margin-top: 0;
}

.computed-panel pre {
    margin: 0;
    overflow-x: auto;
    color: var(--accent);
}

.position-demo {
    position: relative;
    min-height: 300px;
    margin-top: 28px;
    overflow: hidden;
    border: 1px solid #d6dfe5;
    border-radius: var(--radius);
    background: white;
}

.sticky-label {
    position: sticky;
    top: 68px;
    z-index: 2;
    width: fit-content;
    padding: 10px 14px;
    border-radius: 0 0 10px 0;
    color: #061019;
    background: var(--accent);
    font-weight: 800;
}

.position-demo-content {
    max-width: 720px;
    padding: 32px;
    color: #596b78;
}

.table-wrapper {
    overflow-x: auto;
    border: 1px solid #d6dfe5;
    border-radius: var(--radius);
    background: white;
}

table {
    width: 100%;
    min-width: 760px;
    border-collapse: collapse;
}

th,
td {
    padding: 16px;
    border-bottom: 1px solid #e2e8ec;
    text-align: left;
    vertical-align: top;
}

th {
    color: #33434f;
    background: #f7f9fa;
}

.accessibility-note {
    margin-top: 28px;
    padding: 24px;
    border-left: 4px solid var(--accent-strong);
    border-radius: 0 12px 12px 0;
    background: white;
}

.accessibility-note h3 {
    margin-top: 0;
}

.site-footer {
    padding-block: 32px;
    color: var(--muted);
    background: var(--bg);
}

@media (max-width: 900px) {
    .concept-grid,
    .position-grid {
        grid-template-columns: repeat(2, 1fr);
    }

    .playground {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 620px) {
    .container {
        width: min(calc(100% - 24px), var(--max-width));
    }

    .header-content {
        align-items: flex-start;
        flex-direction: column;
        padding-block: 14px;
    }

    .hero h1 {
        font-size: clamp(2.8rem, 15vw, 4.5rem);
    }

    .concept-grid,
    .position-grid {
        grid-template-columns: 1fr;
    }

    .section {
        padding-block: 64px;
    }

    .demo-container {
        min-height: 380px;
        padding: 24px;
    }
}
